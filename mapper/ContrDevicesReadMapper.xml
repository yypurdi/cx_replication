<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="readContrDevicesMapper">			
	
	<select id="readContrDevices">
		select * from contr_devices where cd_id = #{cdId};
	</select>

	<select id="port_T311Q">         
		select * from port where cd_id = #{cdId};
	</select>
  
</mapper>
