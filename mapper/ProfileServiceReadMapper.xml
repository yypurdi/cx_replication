<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="readProfileServiceMapper">			
	
	<select id="readProfileService">
		select * from profile_service where profile_id = #{profileId};
	</select>

	<select id="prServSpcodeHist_NBND4">         
		select * from pr_serv_spcode_hist where profile_id = #{profileId};
	</select>
  
	<select id="prServStatusHist_W5SOL">         
		select * from pr_serv_status_hist where profile_id = #{profileId};
	</select>
  
</mapper>
